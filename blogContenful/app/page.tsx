// App.js

import LoginForm from './component/logintoken';

const App = () => {
  //const [user, setUser] = useState(null);

  // const handleLogin = (username) => {
  //   setUser(username); // Set the user in the state to the logged-in username
  // };

  // const handleLogout = () => {
  //   setUser(null); // Clear the user state to indicate logout
  // };

  return (
    <div>

        <LoginForm />
    
    </div>
  );
};

export default App;
